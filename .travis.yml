language: node_js

node_js:
  - "node"

branches:
  only:
    - master
 
script:
 - npm install --save-dev nyc mocha nodemon sinon mongoose request
 - cd backend; nodemon server; cd ..
 - npm run test
 - cd bot; npm install --save-dev nyc mocha; npm run test
 - cd ../productSearch; npm install --save-dev nyc mocha xmlhttprequest; npm run test